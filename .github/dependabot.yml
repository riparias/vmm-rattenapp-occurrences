version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Optioneel: groepeer updates in één PR per week
    groups:
      github-actions:
        patterns:
          - "*"
